# Core
fastapi
uvicorn[standard]

# Database (async SQLAlchemy + Supabase Postgres)
sqlalchemy[asyncio]
asyncpg

# Config / settings
pydantic-settings

# Auth
fastapi-users[sqlalchemy]

# AI — swap engine (PydanticAI + Groq / Llama 3.1 8B Instant)
pydantic-ai-slim[groq]

# --- added per-module as we build ---
# pydantic-ai                 # Swap engine
# redis                       # caching (Upstash)
# httpx                       # USDA / external API calls
